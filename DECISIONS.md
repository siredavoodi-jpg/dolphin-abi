# تصمیم‌ها

## ۱۴۰۵/۰۵/۱۸ — نمونه مرحله اول
- **تصمیم:** داشبورد فارسی RTL با داده نمونه برای اعتبارسنجی تجربه مدیر شعبه.
- **گزینه‌ها:** لندینگ عمومی، پنل عضو، داشبورد مدیریت.
- **دلیل:** داشبورد مدیریت بیشترین نیازمندی‌ها را هم‌زمان قابل ارزیابی می‌کند.
- **وضعیت فعلی:** تصمیم دیتابیس و پرداخت حضوری اجرا شده؛ RFID همچنان به فاز بعد منتقل است.

## ۱۴۰۵/۰۵/۱۸ — معماری MVP تأییدشده
- **چندمستاجری:** shared schema با `organization_id` و RLS.
- **ورود:** ساخت حساب توسط ادمین، username و رمز موقت، تغییر رمز در اولین ورود.
- **پرداخت:** ثبت حضوری نقدی، کارت‌خوان یا انتقال؛ بدون درگاه آنلاین.
- **RFID:** اتصال سخت‌افزاری به فاز بعد منتقل شد.

## ۱۴۰۵/۰۶/۱۴ — تثبیت فاز صفر

- **مخزن مرجع:** GitHub با آدرس `siredavoodi-jpg/dolphin-abi`.
- **قفل وابستگی‌ها:** npm و `package-lock.json` برای ساخت تکرارپذیر.
- **مهاجرت دیتابیس:** ساختار اجراشده فعلی به‌عنوان baseline ثبت می‌شود؛ پیش از push به پروژه موجود باید نسخه baseline در تاریخچه مهاجرت‌ها «اعمال‌شده» علامت‌گذاری شود.
- **بازیابی رمز:** در MVP بازیابی عمومی با ایمیل نداریم؛ مدیر رمز کاربران را از پنل کاربران بازنشانی می‌کند و بازیابی مدیر کل فقط از مسیر امن Supabase انجام می‌شود.
- **پرداخت آنلاین:** عمداً خارج از MVP اول باقی می‌ماند.
