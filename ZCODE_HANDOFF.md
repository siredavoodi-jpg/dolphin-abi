# سند جامع تحویل پروژه «دلفین آبی» به Zcode

> تاریخ تهیه: ۱۴ شهریور ۱۴۰۵ / ۵ سپتامبر ۲۰۲۶  
> وضعیت سند: شرح کامل وضعیت موجود، روش اجرا، معماری، سوابق و نقشه ادامه MVP

## ۱. معرفی پروژه

«دلفین آبی» یک سامانه فارسی و راست‌به‌چپ برای مدیریت مجموعه‌های آبی و استخرهاست. چشم‌انداز نهایی محصول، مدیریت یکپارچه اعضا، کاربران، شعب، عضویت‌ها، سانس‌ها، رزرو، پذیرش، پرداخت، کیف پول، RFID/QR، فروشگاه، انبار و گزارش‌هاست.

تصمیم مهم MVP این است که **در فاز اول درگاه پرداخت آنلاین وجود ندارد**. مدیر حساب کاربران را ایجاد می‌کند، نام کاربری و رمز موقت را به آن‌ها تحویل می‌دهد و کاربر در اولین ورود مجبور به انتخاب رمز شخصی می‌شود. پرداخت‌ها در این فاز فقط به‌صورت حضوری شامل نقد، کارت‌خوان یا انتقال بانکی ثبت می‌شوند.

## ۲. محل پروژه و آدرس‌های اصلی

### محل نصب روی رایانه

```text
C:\Users\Ehsan\Documents\Codex\2026-08-09\sites-plugin-sites-openai-bundled-create-2
```

### مخزن GitHub

```text
https://github.com/siredavoodi-jpg/dolphin-abi.git
```

### نسخه مستقر روی Vercel

```text
https://dolphin-abi.vercel.app
```

صفحه ورود:

```text
https://dolphin-abi.vercel.app/login
```

پروژه Vercel در فضای کاری `afra-lawyers-club` با نام `dolphin-abi` ایجاد و به مخزن GitHub متصل شده است.

### پروژه Supabase

```text
Project name: Dolphin Abi
Project ref: zhowwkyvaakelmznvjef
Region: eu-central-1 (Frankfurt)
PostgreSQL: 17
Project URL: https://zhowwkyvaakelmznvjef.supabase.co
```

کلید عمومی Supabase در `lib/supabase-browser.ts` قرار دارد. این کلید publishable است. هیچ service-role key، secret key یا رمز عبور نباید در مخزن، این سند یا کد مرورگر ثبت شود.

## ۳. فناوری‌ها

- Next.js 16.3.4 با App Router
- React 19
- TypeScript
- Supabase Auth
- Supabase PostgreSQL 17
- Supabase Row Level Security یا RLS
- Supabase Edge Functions
- Vercel برای میزبانی رابط کاربری
- npm و `package-lock.json` برای نصب تکرارپذیر
- `lucide-react` برای آیکن‌ها
- رابط فارسی RTL

## ۴. ساختار مهم پوشه‌ها و فایل‌ها

```text
app/                         صفحات و رابط کاربری Next.js
  login/                     ورود با نام کاربری و رمز
  change-password/           تغییر اجباری رمز در اولین ورود
  users/                     کاربران و دسترسی‌ها
  branches/                  مدیریت شعب
  members/                   مدیریت اعضا
  memberships/               طرح‌ها و عضویت اعضا
  payments/                  پرداخت‌های حضوری و ابطال
  sessions/                  سانس‌ها و رزرو
  attendance/                ثبت ورود و خروج
  page.tsx                   داشبورد اصلی؛ فعلاً بخشی از داده‌ها نمایشی است

lib/
  supabase-browser.ts         آدرس و کلید عمومی Supabase و client مرورگر

supabase/
  functions/                 Edge Functionهای امن سمت سرور
  migrations/                مهاجرت‌های استاندارد دیتابیس

database/
  schema.sql                 snapshot قابل مطالعه از ساختار فعلی دیتابیس

docs/
  DATABASE_DESIGN.md          طراحی و قواعد دیتابیس
  ADMIN_ACCESS_RECOVERY.md    روش بازیابی دسترسی مدیر

PRODUCT.md                   چشم‌انداز و محدوده MVP
ARCHITECTURE.md              معماری فعلی
DECISIONS.md                 تصمیم‌های کلیدی پروژه
PROGRESS.md                  تاریخچه پیشرفت
README.md                    معرفی و روش اجرای کوتاه
package.json                 وابستگی‌ها و فرمان‌ها
package-lock.json            قفل نسخه وابستگی‌ها
vercel.json                  تنظیم ساخت Vercel
.openai/hosting.json         شناسه پروژه قدیمی/موازی Sites
```

## ۵. روش اجرای پروژه روی رایانه

### پیش‌نیازها

- Node.js نسخه 20.9 یا بالاتر
- npm
- Git
- دسترسی اینترنت برای اولین نصب وابستگی‌ها

### نصب وابستگی‌ها

PowerShell را در پوشه اصلی پروژه باز کنید و اجرا کنید:

```powershell
npm install
```

اگر PowerShell اجرای `npm.ps1` را مسدود کرد، از فایل اجرایی npm استفاده کنید:

```powershell
& "D:\Program Files\nodejs\npm.cmd" install
```

### اجرای محیط توسعه

```powershell
npm run dev
```

سپس آدرس چاپ‌شده در ترمینال، معمولاً آدرس زیر، باز شود:

```text
http://localhost:3000
```

### کنترل TypeScript

```powershell
npm run typecheck
```

### ساخت نسخه تولید

```powershell
npm run build
```

### اجرای خروجی تولید روی رایانه

```powershell
npm run start
```

## ۶. وضعیت Git و نکته بسیار مهم مخزن محلی

در حال حاضر دو پوشه Git در محل پروژه وجود دارد:

1. `.git` در ریشه پروژه که یک مخزن خالی و اشتباهی است.
2. `work/site.git` که تاریخچه واقعی پروژه و remote گیت‌هاب را نگه می‌دارد.

remote ثبت‌شده در تاریخچه واقعی:

```text
https://github.com/siredavoodi-jpg/dolphin-abi.git
```

به دلیل محدودیت دسترسی ویندوز، انتقال خودکار پوشه‌ها انجام نشد. تا زمان اصلاح، فرمان‌های Git باید به شکل زیر اجرا شوند:

```powershell
git --git-dir="work/site.git" --work-tree="." status
git --git-dir="work/site.git" --work-tree="." log --oneline -10
```

برای اصلاح دائمی، ابتدا از هر دو پوشه نسخه پشتیبان گرفته شود، سپس مخزن خالی ریشه کنار گذاشته و `work/site.git` به `.git` منتقل شود. این کار نباید با حذف کورکورانه، `git reset --hard` یا overwrite انجام شود.

آخرین commitهای موجود در تاریخچه محلی واقعی پیش از تغییرات فاز صفر:

```text
daae2c7 Allow Vercel origin across Supabase functions
5f0059b Fix production username login on Vercel
602474b Configure production deployment on Vercel
b226b69 Add member attendance desk
```

تغییرات فاز صفر تهیه‌شده در ۵ سپتامبر ۲۰۲۶ هنوز باید بازبینی، commit، push و سپس deploy شوند.

## ۷. مدل ورود و امنیت

### جریان ورود

1. مدیر کل از صفحه «کاربران و دسترسی‌ها» کاربر را می‌سازد.
2. سیستم یک username و رمز موقت قوی ایجاد می‌کند.
3. رمز موقت فقط یک‌بار در پنل مدیر نمایش داده می‌شود.
4. کاربر از `/login` وارد می‌شود.
5. اگر `must_change_password=true` باشد، کاربر به `/change-password` هدایت می‌شود.
6. کاربر یک رمز حداقل ۱۲ کاراکتری شامل حرف کوچک، حرف بزرگ و عدد انتخاب می‌کند.
7. پس از تغییر رمز، کاربر وارد داشبورد می‌شود.

### نقش‌های فعلی

- `owner`: مدیر کل
- `branch_manager`: مدیر شعبه
- `receptionist`: پذیرش
- `member`: عضو

### کنترل دسترسی

- داده‌ها با `organization_id` از یکدیگر جدا می‌شوند.
- معماری چندمستاجری از نوع shared schema + RLS است.
- ۲۶ policy سطح سطر در دیتابیس اجرا شده است.
- کاربر authenticated بدون عضویت سازمانی در آزمون انجام‌شده هیچ رکورد سازمانی مشاهده نکرد.
- هیچ دسترسی ناشناس به جدول‌های MVP داده نشده است.
- عملیات مدیریتی حساس در Edge Function و با service-role سمت سرور انجام می‌شود.

### بازیابی رمز

- مدیر کل می‌تواند رمز کاربران غیرمالک را از صفحه کاربران بازنشانی کند.
- برای مدیر کل که رمز خود را فراموش کرده، در MVP بازیابی عمومی ایمیلی وجود ندارد؛ چون حساب‌ها با ایمیل داخلی/مصنوعی ساخته می‌شوند.
- بازیابی اضطراری مدیر باید از Supabase Authentication انجام شود و سپس `must_change_password` در جدول `profiles` برابر `true` شود.
- جزئیات کامل در `docs/ADMIN_ACCESS_RECOVERY.md` آمده است.
- پیش از پایلوت واقعی بهتر است حداقل دو حساب owner فعال وجود داشته باشد.

هیچ credential فعالی در این سند درج نشده است. رمز قدیمی احتمالی نباید قابل اتکا تلقی شود و در صورت نیاز باید امن بازنشانی شود.

## ۸. دیتابیس اجراشده

ساختار MVP روی Supabase اجرا شده و شامل ۱۲ جدول اصلی است:

1. `organizations`
2. `branches`
3. `profiles`
4. `organization_users`
5. `members`
6. `membership_plans`
7. `memberships`
8. `payment_records`
9. `pool_sessions`
10. `session_reservations`
11. `attendance_events`
12. `audit_logs`

اصول دیتابیس:

- شناسه‌ها UUID هستند.
- زمان‌ها UTC و از نوع `timestamptz` هستند.
- مبلغ‌ها عدد صحیح و با واحد IRR ذخیره می‌شوند.
- پرداخت و ابطال مالی باید قابل ردیابی باشند.
- ارتباط شعب و سازمان‌ها با foreign key کنترل می‌شود.
- RLS روی تمام جدول‌های سازمانی فعال است.
- extensionهای `citext` و `pgcrypto` استفاده شده‌اند.
- ایندکس‌های foreign key بر اساس Supabase Advisor اصلاح شده‌اند.
- آخرین بررسی Advisor در مرحله اجرای schema هشدار امنیتی نداشت.

### migration خط مبنا

فایل زیر snapshot ساختار فعلی را به migration استاندارد تبدیل کرده است:

```text
supabase/migrations/20260905160516_baseline_mvp_schema.sql
```

نکته مهم: این schema قبلاً روی دیتابیس تولید اجرا شده است. بنابراین **نباید baseline دوباره مستقیماً روی همان دیتابیس اجرا شود**. قبل از push مهاجرت‌های بعدی، نسخه baseline باید در migration history پروژه فعلی به‌عنوان applied علامت‌گذاری شود. برای دیتابیس جدید و خالی، baseline از ابتدا اجرا می‌شود.

## ۹. Edge Functionهای Supabase

نه تابع زیر در سورس پروژه وجود دارد:

1. `username-login`
2. `change-password`
3. `admin-users`
4. `admin-branches`
5. `admin-members`
6. `admin-memberships`
7. `admin-payments`
8. `admin-sessions`
9. `admin-attendance`

کارکرد کلی:

- تبدیل username به هویت داخلی و ورود امن
- تغییر رمز و برداشتن الزام تغییر رمز
- ایجاد کاربر، تنظیم نقش و شعبه، مسدودسازی و reset password
- CRUD شعب
- CRUD اعضا
- تعریف طرح و صدور عضویت
- ثبت و ابطال پرداخت حضوری
- مدیریت سانس و رزرو
- ثبت ورود و خروج عضو

CORS توابع برای دامنه تولید زیر اصلاح شده است:

```text
https://dolphin-abi.vercel.app
```

و برای توسعه محلی نیز originهای localhost مجاز نگه داشته شده‌اند.

## ۱۰. صفحات پیاده‌سازی‌شده

| مسیر | وضعیت | توضیح |
|---|---|---|
| `/login` | متصل | ورود با username و password |
| `/change-password` | متصل | تغییر اجباری رمز اولین ورود |
| `/users` | متصل | ایجاد کاربر، نقش، وضعیت و reset password |
| `/branches` | متصل | مدیریت شعب |
| `/members` | متصل | ثبت و مدیریت اعضا |
| `/memberships` | متصل | طرح عضویت و صدور عضویت |
| `/payments` | متصل | ثبت پرداخت حضوری و ابطال |
| `/sessions` | متصل | سانس‌ها و رزرو |
| `/attendance` | متصل | ورود و خروج اعضا |
| `/` | نیمه‌نمایشی | چیدمان کامل است، اما آمار و نمودارها هنوز mock هستند |

صفحات فروشگاه، انبار و گزارش‌ها در منو دیده می‌شوند، ولی منطق کامل آن‌ها هنوز پیاده‌سازی نشده است.

## ۱۱. اقداماتی که تاکنون انجام شده‌اند

### تحلیل و طراحی

- بریف محصول تحلیل شد.
- نقش‌ها و جریان‌های اصلی مشخص شدند.
- معماری چندمستاجری shared schema با `organization_id` انتخاب شد.
- فاز اول بدون درگاه پرداخت و RFID تصویب شد.
- طراحی رابط فارسی RTL و داشبورد مدیریتی انجام شد.

### Supabase

- پروژه Supabase در فرانکفورت ایجاد شد.
- PostgreSQL 17 فعال شد.
- ۱۲ جدول و ۲۶ policy اجرا شدند.
- extension و search path اصلاح شدند.
- ایندکس‌های پوششی foreign key اضافه شدند.
- هم‌پوشانی policyهای permissive رفع شد.
- آزمون جداسازی داده RLS انجام شد.
- داده‌های آزمایشی با rollback پاک شدند.
- ۹ Edge Function پیاده‌سازی و نسخه‌های اصلاح‌شده deploy شدند.

### ورود و کاربران

- ورود با نام کاربری پیاده‌سازی شد.
- رمز موقت و تغییر اجباری رمز اولین ورود پیاده‌سازی شد.
- مدیر اولیه ساخته شد.
- مدیریت کاربران، نقش‌ها، شعبه کاربر، مسدودسازی و reset password اضافه شد.
- CORS ورود و تغییر رمز برای دامنه Vercel اصلاح شد.

### ماژول‌های عملیاتی

- مدیریت شعب
- مدیریت اعضا
- طرح‌های عضویت و صدور عضویت
- پرداخت حضوری و ابطال پرداخت
- مدیریت سانس
- رزرو سانس
- ثبت ورود و خروج

### استقرار

- مخزن GitHub ایجاد و سورس اولیه در آن ثبت شد.
- Vercel به مخزن متصل شد.
- اولین deployment تولید انجام شد.
- سایت روی `dolphin-abi.vercel.app` در دسترس قرار گرفت.

### تثبیت فاز صفر

- `package-lock.json` تولید شد.
- script بررسی TypeScript اضافه شد.
- `lucide-react` از نسخه ناسازگار قدیمی به 1.41.0 ارتقا یافت.
- Next.js از 16.2.11 آسیب‌پذیر به 16.3.4 ارتقا یافت.
- TypeScript config با Next.js 16.3 هماهنگ شد.
- build نهایی همه ۱۱ route را با موفقیت تولید کرد.
- `npm audit` پس از ارتقا، صفر آسیب‌پذیری گزارش کرد.
- migration baseline ساخته شد.
- مستندات محصول، معماری، تصمیم‌ها، پیشرفت و بازیابی مدیر به‌روزرسانی شدند.

## ۱۲. خطاهای مهم مشاهده‌شده و راه‌حل‌ها

### خطای ورود با username در Vercel

علت: کلید عمومی یا فراخوانی تابع ورود در محیط تولید با تنظیمات ناهماهنگ بود.  
راه‌حل: استفاده یکپارچه از `supabasePublishableKey` و اصلاح تابع `username-login`.

### خطای CORS در change-password

نشانه: درخواست preflight موفق بود اما POST با پیام `Access-Control-Allow-Origin` اشتباه مسدود می‌شد.  
راه‌حل: اضافه‌کردن origin دقیق `https://dolphin-abi.vercel.app` به تمام Edge Functionهای مربوط و deploy مجدد.

### نبودن GitHub CLI

در PowerShell فرمان `gh auth login` شناخته نشد، چون GitHub CLI نصب نبود. اتصال از طریق افزونه GitHub و مخزن remote انجام شد. برای کار عادی Git الزام به `gh` وجود ندارد.

### نبود lockfile و ناسازگاری وابستگی

هنگام ساخت lockfile مشخص شد `lucide-react` قدیمی با React 19 ناسازگار است. پس از ارتقا و build مجدد رفع شد.

### هشدار امنیتی npm

Next.js 16.2.11 وابستگی‌های آسیب‌پذیر PostCSS و Sharp داشت. Next.js به 16.3.4 ارتقا یافت و بررسی نهایی صفر vulnerability گزارش کرد.

### مشکل npm در کش ویندوز

npm اجازه نوشتن در کش سراسری کاربر را نداشت. برای عملیات محلی از کش داخل پروژه استفاده شد:

```powershell
npm install --cache .npm-cache
```

پوشه `.npm-cache/` در `.gitignore` قرار گرفته است.

### مشکل ساختار Git محلی

مخزن خالی `.git` در ریشه با تاریخچه واقعی `work/site.git` تداخل دارد. انتقال خودکار به علت Access Denied ویندوز انجام نشد. این مسئله هنوز باز است و باید پیش از کار جدی تیمی اصلاح شود.

## ۱۳. وضعیت کیفیت و آزمون

آخرین کنترل محلی فاز صفر:

```text
TypeScript: PASS
Next.js production build: PASS
Routes generated: 11 application routes + not-found
npm audit: 0 vulnerabilities
Database RLS isolation test: PASS در مرحله اجرای schema
Supabase Advisor security warnings: 0 در آخرین بررسی ثبت‌شده
```

هنوز test suite خودکار، CI و تست مرورگر end-to-end به مخزن اضافه نشده‌اند. این موارد قبل از عرضه عمومی لازم‌اند.

## ۱۴. وضعیت کلی پیشرفت

برآورد تقریبی:

- چشم‌انداز کامل تجاری محصول: حدود ۳۰ تا ۳۵ درصد
- MVP محدود مدیریتی بدون درگاه پرداخت: حدود ۷۰ تا ۷۵ درصد

دلیل اختلاف این دو عدد این است که قسمت بزرگی از امکانات آینده مانند پنل عضو، کیف پول، فروشگاه، انبار، RFID، گزارش‌های تجمیعی و پرداخت آنلاین عمداً خارج از MVP اول هستند.

## ۱۵. فازبندی پیشنهادی ادامه MVP

### فاز صفر — تثبیت فنی

وضعیت: تقریباً انجام‌شده؛ commit/push/deploy تغییرات جدید و اصلاح نهایی `.git` باقی مانده است.

- یکپارچه‌سازی ساختار Git محلی
- lockfile و build تکرارپذیر
- migration baseline
- ارتقای امنیت وابستگی‌ها
- مستندسازی بازیابی مدیر
- به‌روزرسانی مستندات وضعیت واقعی

### فاز یک — داشبورد واقعی و گزارش پایه

هدف: حذف داده‌های mock از صفحه اصلی.

- آمار اعضای فعال از دیتابیس
- درآمد امروز از `payment_records`
- تعداد مراجعات امروز از `attendance_events`
- ظرفیت و وضعیت سانس فعال
- فهرست سانس‌های امروز
- فعالیت‌های اخیر از داده واقعی یا `audit_logs`
- فیلتر بر اساس سازمان و شعبه مجاز کاربر
- حالت loading، empty state و error state

خروجی قابل قبول: مدیر پس از ورود فقط آمار واقعی مجموعه خود را ببیند.

### فاز دو — تکمیل جریان عملیاتی پذیرش

- صفحه جزئیات عضو
- جستجوی یکپارچه عضو با نام، تلفن، کد ملی و شماره عضویت
- ویرایش امن اطلاعات عضو
- نمایش تاریخچه عضویت‌ها، پرداخت‌ها، رزروها و ورود/خروج
- کنترل ظرفیت هم‌زمان هنگام رزرو و ورود
- رسید قابل چاپ برای عضویت و پرداخت حضوری
- بهبود پیام‌های خطا و تأیید عملیات حساس

خروجی قابل قبول: اپراتور پذیرش بتواند کل چرخه عضو را بدون مراجعه به Supabase انجام دهد.

### فاز سه — گزارش، حسابرسی و پایداری

- گزارش درآمد روزانه، هفتگی و ماهانه
- گزارش روش پرداخت و ابطال‌ها
- گزارش اعضای فعال، منقضی و نزدیک انقضا
- گزارش حضور و ظرفیت سانس‌ها
- تکمیل ثبت رویدادها در `audit_logs`
- خروجی CSV یا Excel
- تست‌های واحد و integration
- تست end-to-end جریان اصلی
- CI برای typecheck، build و audit
- مانیتورینگ خطا و لاگ تولید

خروجی قابل قبول: مدیر بتواند عملیات پایلوت را کنترل و مغایرت‌ها را ردیابی کند.

### فاز چهار — آماده‌سازی پایلوت

- ایجاد حداقل دو حساب owner
- تعریف شعبه و کاربران واقعی آزمایشی
- ورود داده محدود و کنترل‌شده
- تست چندنقشی owner، manager و receptionist
- پشتیبان‌گیری و سناریوی بازیابی
- تست موبایل، تبلت و دسکتاپ
- بررسی دسترس‌پذیری و سرعت
- آموزش کوتاه کاربران پایلوت
- رفع خطاهای حاصل از استفاده واقعی

خروجی قابل قبول: یک شعبه بتواند برای مدت محدود با MVP کار واقعی انجام دهد.

### فازهای پس از MVP

- پنل عضو
- ورود با QR و اتصال RFID
- کیف پول و تراکنش‌ها
- درگاه پرداخت آنلاین
- فروشگاه و سفارش
- انبار و گردش کالا
- اعلان پیامکی/ایمیلی
- اشتراک SaaS و مدیریت پلن مجموعه‌ها
- گزارش‌های تجمیعی چندمجموعه‌ای

## ۱۶. کارهای فوری پیشنهادی برای Zcode

ترتیب پیشنهادی:

1. از کل پوشه پروژه و هر دو پوشه Git پشتیبان بگیرد.
2. مشکل `.git` ریشه و `work/site.git` را بدون از دست‌دادن تاریخچه اصلاح کند.
3. diff تغییرات فاز صفر را بازبینی کند.
4. `npm install`، سپس `npm run typecheck` و `npm run build` را اجرا کند.
5. صحت `npm audit` با نتیجه صفر را کنترل کند.
6. baseline دیتابیس را روی پروژه موجود دوباره اجرا نکند؛ ابتدا migration history را repair/mark applied کند.
7. تغییرات فاز صفر را commit و push کند.
8. deployment جدید Vercel را بررسی کند.
9. جریان ورود و تغییر رمز را روی دامنه تولید آزمایش کند.
10. فاز یک را با اتصال داشبورد به داده واقعی آغاز کند.

## ۱۷. معیار پذیرش MVP

MVP زمانی آماده پایلوت محسوب می‌شود که:

- مدیر بتواند کاربر بسازد و رمز موقت تحویل دهد.
- کاربر بتواند ورود اول و تغییر رمز را بدون خطا انجام دهد.
- هر نقش فقط داده‌ها و عملیات مجاز خود را ببیند.
- ثبت شعبه، عضو، طرح، عضویت، پرداخت، سانس، رزرو و حضور کامل باشد.
- داشبورد از داده واقعی استفاده کند.
- پرداخت آنلاین در هیچ جریان ضروری فرض نشده باشد.
- عملیات مالی قابل ابطال و ردیابی باشد.
- جریان اصلی end-to-end تست شده باشد.
- build، audit و کنترل RLS موفق باشند.
- نسخه تولید قابل بازگشت و پایش باشد.

## ۱۸. مواردی که نباید انجام شوند

- قراردادن service-role key یا رمز در GitHub، Vercel client env، فایل‌های frontend یا پیام‌ها
- اجرای دوباره baseline روی دیتابیس تولید موجود
- استفاده از `git reset --hard` برای حل مشکل مخزن محلی
- force push قبل از مقایسه تاریخچه محلی و GitHub
- فعال‌کردن درگاه پرداخت در MVP بدون طراحی مالی و webhook
- اجازه دسترسی مستقیم و گسترده مرورگر به عملیات مدیریتی
- حذف RLS برای ساده‌کردن توسعه
- استفاده از حساب owner به‌عنوان حساب روزمره پذیرش

## ۱۹. اطلاعات و تصمیم‌هایی که Zcode باید حفظ کند

- زبان رابط فارسی و جهت RTL است.
- فاز اول بدون درگاه پرداخت است.
- حساب کاربر توسط مدیر ساخته می‌شود.
- اولین تغییر رمز اجباری است.
- معماری داده چندمستاجری و مبتنی بر `organization_id` است.
- RLS و Edge Function مرز امنیتی اصلی هستند.
- هیچ secret در repository ذخیره نمی‌شود.
- Vercel مقصد فعلی رابط کاربری و Supabase مقصد دیتابیس/احراز هویت است.
- داشبورد اصلی هنوز منبع داده واقعی ندارد و اولین اولویت توسعه است.

## ۲۰. جمع‌بندی تحویل

پروژه از مرحله نمونه صرف عبور کرده و هسته مدیریتی متصل به Supabase دارد. ورود، کاربران، شعب، اعضا، عضویت، پرداخت حضوری، سانس، رزرو و پذیرش پیاده‌سازی شده‌اند. build و بررسی امنیت وابستگی‌ها در آخرین آزمون موفق بوده است. مهم‌ترین کارهای باقی‌مانده برای MVP عبارت‌اند از اصلاح نهایی مخزن محلی، ثبت و انتشار تغییرات فاز صفر، اتصال داشبورد به داده واقعی، تکمیل جزئیات عضو و گزارش‌های پایه، و افزودن تست end-to-end و CI.

این سند باید همراه با فایل‌های `PRODUCT.md`، `ARCHITECTURE.md`، `DECISIONS.md`، `PROGRESS.md`، `docs/DATABASE_DESIGN.md` و `docs/ADMIN_ACCESS_RECOVERY.md` به Zcode داده شود.
